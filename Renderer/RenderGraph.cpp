#include "RenderGraph.h"

namespace rkg {
namespace render {
RenderGraphResource ResourceManager::CreateBuffer(const BufferCreationInfo & info)
{

    return RenderGraphResource();
}

RenderGraphResource ResourceManager::ReadBuffer(RenderGraphResource)
{
    return RenderGraphResource();
}

RenderGraphResource ResourceManager::WriteBuffer(RenderGraphResource)
{
    return RenderGraphResource();
}


}//namespace render;
}//namespace rkg

